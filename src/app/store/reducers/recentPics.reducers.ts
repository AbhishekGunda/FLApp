// import { UserActions } from '../actions/user.actions';
import { initialRecentPicsState } from '../state/app.state';

export const recentPicsReducer = (
  state = initialRecentPicsState,
  action: AddRecentPics,
): any => {
  switch (true) {
    

    default:
      return state;
  }
};
