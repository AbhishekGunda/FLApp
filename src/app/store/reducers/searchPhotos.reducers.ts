// import { UserActions } from '../actions/user.actions';
import { initialSearchPhotosState } from '../state/app.state';

export const SearchPhotosReducer = (
  state = initialSearchPhotosState,
): any => {
  switch (true) {


    default:
      return state;
  }
};
